'''Classe Retangulo: Crie uma classe que modele um retangulo:

Atributos: LadoA, LadoB (ou Comprimento e Largura, ou Base e Altura, a escolher)
Métodos: Mudar valor dos lados, Retornar valor dos lados, calcular Área e calcular Perímetro;
Crie um programa que utilize esta classe. Ele deve pedir ao usuário que informe as medidades de um local. Depois, deve criar um objeto com as medidas e calcular a quantidade de pisos e de rodapés necessárias para o local.'''

'''class Retangulo(object): 
    def __init__(self, base, altura):
        self.base = base 
        self.altura = altura    
   
    def trocaLado(self):
        return "A Base do Retângulo agora é {} e a Altura é: {}".format(self.base, self.altura)       

    def mostraLado(self):
        return self.base, "e", self.altura
    
    def mostraArea(self):
        return self.base * self.altura
    
    def mostraPerimetro(self):
        return (self.base + self.altura)*2
        
    
base_local = float(input("Digite um valor para a Base Local: "))
altura_local = float(input("Digite um valor para a Altura Local: "))

base_piso = float(input("Digite um valor para a Base do piso: "))
altura_piso = float(input("Digite um valor para a Altura do rodapé: "))

retangulo_local = Retangulo(base_local, altura_local)'''
# area_local = base_local * altura_local'''